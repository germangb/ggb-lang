// compute the first elements of the fibonacci sequence (the ones that fit in a single byte)
static RESULT:[u8 15]

(= ([0]RESULT) 1)
(= ([1]RESULT) 1)

for offset:&u8 in 2..13 {
    // compute next number in sequence
    (= ([offset]RESULT) (+ ([(- offset 1)]RESULT)
                           ([(- offset 2)]RESULT)))
}

// RESULT[0] = 1
// RESULT[1] = 1
//
// for offset:&u8 in 2..13 {
//     // compute next number in sequence
//     RESULT[offset] = RESULT[offset-1] + RESULT[offset-2]
// }
